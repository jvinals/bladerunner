# ─── Careexpand Pilot Environment Configuration ───────────────────────────────

# Backend
API_PORT=3001
API_HOST=0.0.0.0
NODE_ENV=development

# Database (future — currently using in-memory mock data)
# DATABASE_URL=postgresql://pilot:pilot@localhost:5432/pilot

# Frontend
VITE_API_URL=http://localhost:3001

# Auth (future)
# JWT_SECRET=your-secret-key
# JWT_EXPIRATION=24h

# Orchestrator Integration (future placeholder)
# ORCHESTRATOR_URL=http://localhost:4000
# ORCHESTRATOR_API_KEY=
